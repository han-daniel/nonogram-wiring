# Nonogram wiring

Three pages: intro.html (Introduction), play.html (Demo) and replay.html (Replay).

Replay opens empty. Drop an attempts CSV on it to replay those attempts. The file is read in the browser and never uploaded.

This repository holds no student data.
